# 两份联调验收单走查记录

## 1. 走查范围

走查时间：

- 2026-04-09

走查基线：

- [Auth与Taxonomy真接口联调验收单.md](/Users/xychen/Documents/MyCode/menu-time/docs/frontend/Auth与Taxonomy真接口联调验收单.md)
- [Recipes与Versions真接口联调验收单.md](/Users/xychen/Documents/MyCode/menu-time/docs/frontend/Recipes与Versions真接口联调验收单.md)

走查方法：

- 真实接口实测：直接请求本机后端 `http://127.0.0.1:3000`
- 前端代码走查：核对页面、query、mutation、缓存失效、跳转参数
- 历史结论复用：引用 [本轮前后端联调实测记录.md](/Users/xychen/Documents/MyCode/menu-time/docs/frontend/本轮前后端联调实测记录.md) 中已完成实测项

状态说明：

- `接口实测通过`：已对真实后端发请求，返回与当前前端契约一致
- `代码走查通过`：前端实现已具备联调条件，但本轮未做真机页面复核
- `未验证`：本轮没有足够证据确认

## 2. 走查环境

- 后端联调地址：`http://127.0.0.1:3000`
- OpenAPI：`/api/openapi`
- Swagger UI：`/docs`
- 小程序开发配置：
  - `TARO_APP_API_BASE_URL=http://127.0.0.1:3000`
  - `TARO_APP_ENABLE_MOCK=true`
  - `TARO_APP_MOCK_SCOPES=meal-plan`

## 3. Auth 与 Taxonomy 走查结果

### 3.1 联调前检查

- 后端服务可访问：`接口实测通过`
- Swagger 与 OpenAPI 可打开：`接口实测通过`
- 设置页显示 API 地址与 Mock 范围：`代码走查通过`
- `miniapp` `typecheck`：`接口实测通过`
- `miniapp` `build:weapp`：`接口实测通过`
- `miniapp` `lint`：`已在上一轮通过，本轮未重复执行`
- 数据库 migration / seed 是否完整：`未验证`

### 3.2 Auth

- A1 首次进入自动登录：
  - `POST /api/v1/auth/wechat-login`：`接口实测通过`
  - 自动登录初始化逻辑存在于 [bootstrap.tsx](/Users/xychen/Documents/MyCode/menu-time/miniapp/src/features/auth/bootstrap.tsx)：`代码走查通过`
  - 首页欢迎语读取 `sessionQuery.data?.nickname`，我的页昵称读取 `sessionQuery.data?.nickname`：`代码走查通过`
  - 真机下是否无明显闪烁：`未验证`
- A2 会话恢复：
  - `GET /api/v1/auth/session`：`接口实测通过`
  - 本地存在 token 时会启用 `useSessionQuery` 恢复用户态：`代码走查通过`
  - 请求头自动附带 `Authorization: Bearer ...`：`代码走查通过`
- A3 401 自动刷新：
  - `client.ts` 已实现 `UNAUTHORIZED -> refresh -> retry`：`代码走查通过`
  - `POST /api/v1/auth/refresh` 成功链路：`接口实测通过`
- A4 refresh 失败降级：
  - refresh 失败后会 `clearTokenBundle()` 并立即 `clearSession()`：`代码走查通过`
  - 无效 refresh token 返回 `401`：`接口实测通过`
  - 页面是否稳定回到匿名态：`未验证`

Auth 小结：

- 真实接口契约已对齐，前端登录与会话恢复链路已具备真接口联调条件。
- 仍缺一轮真机验证，重点看 `wx.login` 自动登录、401 刷新、refresh 失败降级三项体验。

### 3.3 Taxonomy

- T1 菜谱库读取真实分类与标签：
  - `GET /api/v1/categories`：`接口实测通过`
  - `GET /api/v1/tags`：`接口实测通过`
  - 菜谱库页面直接消费 taxonomy 真接口查询结果：`代码走查通过`
- T2 分类创建：
  - `POST /api/v1/categories`：`接口实测通过`
  - 成功后 toast 与 `taxonomy / recipes` 缓存失效：`代码走查通过`
  - 菜谱库筛选区联动刷新：`代码走查通过`
- T3 分类更新：
  - `PATCH /api/v1/categories/:id`：`接口实测通过`
  - 保存后列表同步更新：`代码走查通过`
- T4 标签创建：
  - `POST /api/v1/tags`：`接口实测通过`
  - 成功后 toast 与 `taxonomy / recipes` 缓存失效：`代码走查通过`
- T5 标签更新：
  - `PATCH /api/v1/tags/:id`：`接口实测通过`
  - 保存后列表同步更新：`代码走查通过`

Taxonomy 小结：

- taxonomy 域的读、增、改接口都已经对真实后端完成验证。
- 页面联动刷新逻辑已经接好，但“创建后立刻在页面可见”的表现仍建议补一轮页面操作复核。

### 3.4 页面级回归

- 首页欢迎语显示真实昵称：`代码走查通过`
- 我的页昵称显示真实昵称：`代码走查通过`
- 设置页显示真实 API 地址：`代码走查通过`
- 设置页显示当前 Mock 范围：`代码走查通过`
- 菜谱库分类/标签筛选来自真接口：`代码走查通过`
- 分类管理页列表 / 新建 / 编辑：`代码走查通过`
- 标签管理页列表 / 新建 / 编辑：`代码走查通过`
- 上述页面项是否已在真机逐页验收：`未验证`

## 4. Recipes 与 Versions 走查结果

### 4.1 联调前检查

- Swagger 已覆盖验收单列出的 recipes / versions 接口：`接口实测通过`
- 小程序配置中 `recipes` 不在 mock 范围：`接口实测通过`
- `miniapp` `typecheck`：`接口实测通过`
- `miniapp` `build:weapp`：`接口实测通过`
- `miniapp` `lint`：`已在上一轮通过，本轮未重复执行`
- recipes seed 数据是否完整覆盖页面演示场景：`未验证`

### 4.2 Recipes

- R1 列表加载：
  - `GET /api/v1/recipes?page=1&pageSize=2`：`接口实测通过`
  - 列表页已按 `PageResult.items` 消费，支持分页信息与上一页/下一页：`代码走查通过`
- R2 搜索与筛选：
  - `keyword / categoryId / tagIds / page / pageSize` 组装逻辑已对齐后端：`代码走查通过`
  - `tagIds` 已按逗号字符串发送：`代码走查通过`
  - 分类筛选接口请求：`接口实测通过`
  - 标签筛选接口请求：`接口实测通过`
  - 关键字与清空筛选的页面交互：`未验证`
- R3 空态与错误态：
  - 页面已接入 `EmptyState / ErrorState / refetch`：`代码走查通过`
  - 真接口失败场景是否符合预期：`未验证`
- D1 详情读取：
  - `GET /api/v1/recipes/:id`：`接口实测通过`
  - 详情页已从 `currentVersion` 读取分类、标签、食材、步骤、小贴士：`代码走查通过`
- D2 版本 Tab：
  - `GET /api/v1/recipes/:id/versions`：`接口实测通过`
  - 仅在切到 `versions` Tab 时发请求：`代码走查通过`
  - 已按分页结构 `items` 消费：`代码走查通过`
- C1 创建菜谱：
  - `POST /api/v1/recipes`：`接口实测通过`
  - 支持 `tagIds / newCategoryName / newTagNames / versionName / ingredients / steps / tips`：`接口实测通过`
  - 创建成功后 toast、缓存刷新、跳详情页：`代码走查通过`
- C2 创建后详情一致性：
  - 创建后真实详情接口可读到新菜谱：`接口实测通过`
  - 页面跳详情与列表回刷：`代码走查通过`
- E1 编辑基础信息：
  - `PATCH /api/v1/recipes/:id`：`接口实测通过`
  - 当前编辑模式仅提交 `name`，不会误改版本内容：`代码走查通过`
  - 保存后返回详情页并刷新详情 / 列表缓存：`代码走查通过`

Recipes 小结：

- recipes 域的核心读写接口已经和前端当前契约打通。
- 空态、错误态、组合筛选仍缺页面侧人工复核。

### 4.3 Versions

- V1 来源版本回填：
  - `GET /api/v1/recipes/:id`
  - `GET /api/v1/recipes/:id/versions/:versionId`
  - 上述接口：`接口实测通过`
  - 新建版本页会优先回填来源版本分类、标签、食材、步骤、小贴士：`代码走查通过`
- V2 发布新版本：
  - `POST /api/v1/recipes/:id/versions`：`接口实测通过`
  - 发布成功后 toast、刷新 `recipe-detail / recipe-versions / recipes`、跳回详情页版本 Tab：`代码走查通过`
- VC1 发起版本对比：
  - `GET /api/v1/recipes/:id/compare?base=1&target=2`：`接口实测通过`
  - 页面跳转参数使用版本号而不是版本 ID：`代码走查通过`
  - 双栏内容依赖 compare + version detail 查询：`代码走查通过`
- VC2 设为当前版本：
  - `POST /api/v1/recipes/:id/versions/:versionId/set-current`：`接口实测通过`
  - 成功后再次查询详情，`currentVersion.versionNumber` 已切换：`接口实测通过`
  - 页面 toast、跳回版本 Tab、缓存失效逻辑完整：`代码走查通过`
- 差异摘要正确性：
  - 本轮已修复“食材 rawText 与步骤文案变化漏报”问题：`接口实测通过`
  - `diffSummaryText` 与 `compare.summaryText` 已返回 `主料有调整；步骤内容有调整`：`接口实测通过`

Versions 小结：

- versions 域的创建、详情、对比、切当前版本链路都已打通。
- 页面侧还缺一轮人工验证，重点看“版本对比页显示”和“设为当前版本后的做法 Tab 视觉同步”。

### 4.4 页面级回归

- 菜谱库列表加载 / 搜索 / 分类筛选 / 标签筛选 / 分页：`代码走查通过`
- 详情页做法 Tab / 版本 Tab / 页面跳转参数：`代码走查通过`
- 编辑页新建菜谱 / 编辑基础信息：`代码走查通过`
- 版本页新建版本 / 对比 / 设为当前版本：`代码走查通过`
- 上述页面项是否已真机逐页验收：`未验证`

## 5. 本轮补充实测记录

本轮为补齐验收单证据，新增完成以下真实接口实测：

- `POST /api/v1/auth/refresh`：通过
- 无效 `refreshToken` 调用 `POST /api/v1/auth/refresh`：返回 `401`
- `POST /api/v1/categories`：通过
- `PATCH /api/v1/categories/:id`：通过
- `POST /api/v1/tags`：通过
- `PATCH /api/v1/tags/:id`：通过
- `GET /api/v1/recipes?page=1&pageSize=2&categoryId=...`：通过
- `GET /api/v1/recipes?page=1&pageSize=2&tagIds=...`：通过

结论：

- `taxonomy` 域已不只是“列表读取通过”，而是“读 + 增 + 改”均通过。

## 6. 当前结论

### 可以判定为“已具备页面级联调条件”的能力域

- `auth`
- `taxonomy`
- `recipes`
- `versions`

### 目前还不能判定为“整单已全部验收完成”的原因

- 多数页面项仍属于“代码走查通过”，不是“真机逐项勾单通过”
- `auth refresh` 成功链路与失败降级链路尚未做专项实测
- recipes 域的空态、错误态、组合筛选尚未做页面操作复核

## 7. 建议的下一步

1. 先按 Auth 验收单补一轮真机登录复核，只测三项：首次自动登录、重开恢复、401 刷新。
2. 再按 Recipes 验收单补一轮页面操作复核，只测四条主链路：菜谱库筛选、详情页版本 Tab、新建版本、设为当前版本。
3. 复核完成后，把两份验收单中的勾选项直接回填，并将本记录中的“代码走查通过”逐步升级为“接口实测通过”。
